import Screen from "../../components/Screen";
import { removalInfo } from "../../data/removalData";

function Removal() {
  return <Screen data={removalInfo} pageTitle="Depilação" pageSubtitle="" />;
}

export default Removal;
